# prometheus/prometheus.yml

# Prometheus configuration file
global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Alert rules (loaded from the same directory)
rule_files:
  - "alerts.yml"

# Define the scrape configuration for Prometheus to collect metrics from the anomaly API
scrape_configs:
  - job_name: "anomaly-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
